import 'package:flutter/material.dart';

class SkillProgress {
  final String label;
  final double percent;
  final Color color;

  const SkillProgress({
    required this.label,
    required this.percent,
    required this.color,
  });
}

